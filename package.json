{
    "name": "whiteboard-app-root",
    "version": "1.0.0",
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend-react\"",
        "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend-react"
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}